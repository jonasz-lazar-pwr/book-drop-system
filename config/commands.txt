docker-compose down -v
docker-compose up --build -d

docker exec -it bookdrop-database ls /app/config/scripts

docker exec -it bookdrop-database \
  psql -U bookdrop -d bookdrop -f /app/config/scripts/init_db.sql

docker exec -it bookdrop-database \
  psql -U bookdrop -d bookdrop -f /app/config/scripts/verify_db.sql

docker exec -it bookdrop-backend \
  sh -c "PYTHONPATH=/app python core/verify_models.py"